# Frontend runtime configuration
# Provide the backend base URL so browser-side code can call the API.
# Example:
# NEXT_PUBLIC_API_BASE=https://vscode-internal-XXXX.beta01.cloud.kavia.ai:3001
NEXT_PUBLIC_API_BASE=
